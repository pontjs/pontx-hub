/**
 * IndexNow keys identify a verified host rather than authorizing a secret
 * operation, so the value is intentionally public at the declared URL.
 */
export const INDEXNOW_KEY = "e6360a34cb057648737a6b51cf7751f6";
export const INDEXNOW_KEY_PATH = `/${INDEXNOW_KEY}.txt`;
