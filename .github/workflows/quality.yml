name: Quality

on:
  pull_request:
  push:
    branches: [main]

jobs:
  verify:
    runs-on: ubuntu-latest
    env:
      # The hierarchy loader accepts only an immutable metadata revision. Keep
      # CI on the reviewed hierarchy commit rather than a drifting branch URL.
      METADATA_REPO_COMMIT: 222d8c4fc1e0f4c74ff98e284b715b3c568c61aa
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.14.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm typecheck
      - run: pnpm test
      - run: pnpm search:eval
      - run: pnpm build
      - run: pnpm sdk:verify
